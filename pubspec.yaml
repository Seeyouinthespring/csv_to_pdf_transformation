name: csv_to_pdf_transformation
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  csv: ^5.0.2
  fl_chart: ^0.63.0
  collection: any
  pdf: ^3.10.4
  printing: ^5.11.0
  path_provider: ^2.0.15
  file_picker: ^5.3.2
  flutter_bloc: ^8.1.3
  open_app_file: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
  assets:
    - assets/csv/
    - assets/images/

